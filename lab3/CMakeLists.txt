cmake_minimum_required(VERSION 3.2)   # the minimal CMake version required
project(my_solution)                  # the solution name
add_executable(my_solution lab3.cpp)  # we specify the name of executable and source (cpp) files